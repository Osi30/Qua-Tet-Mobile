<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".QuaTetApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuaTet"
        android:usesCleartextTraffic="true">
        
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".ui.activities.ProductActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activities.OtpActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activities.CartActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activities.CheckoutActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activities.PaymentWebViewActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activities.BillingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activities.AddressActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activities.StoreLocationMapActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.activities.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.StaffChatActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.StaffConversationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.OrderHistoryActivity"
            android:exported="false"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.activities.SettingsActivity"
            android:exported="false"
            android:launchMode="singleTask" />

    </application>

</manifest>
